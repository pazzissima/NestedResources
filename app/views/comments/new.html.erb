<%= form_for @comment do |co| %>

  <%= co.fields_for :comments, @comment.comments.build do |c| %>
    <%= c.label :body, 'Comment text' %>
    <%= c.text_field :body %>
  <% end %>
  <%= f.submit "Add" %>
<% end %>
